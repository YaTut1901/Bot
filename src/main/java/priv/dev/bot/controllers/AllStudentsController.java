package priv.dev.bot.controllers;

import org.telegram.telegrambots.meta.api.objects.Update;

/**
 * Controller that return table with students of current teacher
 */
public class AllStudentsController extends Controller {
    public static String TAG = "all_students";
    @Override
    public String getResponse(Update update) {
        return "asdas";
    }
}
